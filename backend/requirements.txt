# Vocably Backend Dependencies — Kokoro-82M

# Core TTS (pulls in torch, transformers, spacy, misaki, espeakng-loader)
kokoro>=0.9.4

# FastAPI server
fastapi==0.128.0
uvicorn[standard]==0.40.0

# Audio processing
soundfile==0.13.1
numpy>=1.20.0

# Authentication — JWT signing and password utilities
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
